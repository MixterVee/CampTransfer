<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/turtle_transfer_launcher"
        android:label="Turtle Transfer Remote"
        android:roundIcon="@drawable/turtle_transfer_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampTransferRemote"
        android:usesCleartextTraffic="true">

        <!-- MainActivityV30 is the proven-stable activity. Keep branding in the
             manifest/resources rather than wrapping the activity at runtime. -->
        <activity
            android:name=".MainActivityV30"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MonitorService"
            android:exported="false" />
    </application>
</manifest>
